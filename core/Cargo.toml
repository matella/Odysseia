[package]
name = "timeline_core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Moteur de calcul Odysseia : parsing, pipeline, stats et rendu vidéo. Zéro réseau."
repository = "https://github.com/odysseia/odysseia"
publish = false

# Contrainte (§6.1) : aucune dépendance Flutter/Dart ici. Le crate doit
# compiler et se tester seul (`cargo test`), et cibler aussi
# wasm32-unknown-unknown (§6.2).
[dependencies]

[dev-dependencies]

[lints.rust]
missing_docs = "warn"

[lints.clippy]
todo = "allow"
