# Golden tests (§3.10)

Tests golden Flutter dès la v1. Les images de référence sont générées avec
`flutter test --update-goldens` et commitées.

⚠️ Le rendu de police varie selon la plateforme : la CI (Linux) doit être la
référence, sinon les goldens générés en local (Windows/macOS) échouent.
