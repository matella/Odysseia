// Repli théorique : aucune plateforme ne l'utilise, il existe pour que
// l'import conditionnel soit bien formé.

import 'package:drift/drift.dart';

/// Ouvre la base locale.
DatabaseConnection? openConnection() => null;
