// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'import.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `bump`, `flush`, `new`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `StreamingSink`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `assert_receiver_is_total_eq`, `assert_receiver_is_total_eq`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `clone`, `eq`, `eq`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `fmt`, `point`, `progress`, `segment`, `visit`

/// Importe un export Timeline en streaming (§4).
///
/// `max_bytes` applique la limite de taille : la **politique** appartient à
/// l'appelant (500 Mo confortables en natif, avertissement au-delà de
/// ~200 Mo en web, §4), le cœur ne fait que l'appliquer.
Stream<ImportEvent> importTimeline({
  required String path,
  PlatformInt64? maxBytes,
}) => RustLib.instance.api.crateApiImportImportTimeline(
  path: path,
  maxBytes: maxBytes,
);

/// Lot d'enregistrements, envoyé au fil de la lecture.
class ImportChunk {
  /// Positions du lot.
  final List<ImportedPoint> points;

  /// Arrêts du lot.
  final List<ImportedVisit> visits;

  /// Trajets du lot.
  final List<ImportedSegment> segments;

  const ImportChunk({
    required this.points,
    required this.visits,
    required this.segments,
  });

  static Future<ImportChunk> default_() =>
      RustLib.instance.api.crateApiImportImportChunkDefault();

  @override
  int get hashCode => points.hashCode ^ visits.hashCode ^ segments.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportChunk &&
          runtimeType == other.runtimeType &&
          points == other.points &&
          visits == other.visits &&
          segments == other.segments;
}

/// Cause d'échec (§3.10) — un cas, un variant, un message traduit.
enum ImportErrorKind {
  /// L'export ne contient aucune donnée.
  emptyExport,

  /// Le fichier n'est pas du JSON valide.
  corruptJson,

  /// Format inconnu — souvent une version future de Google Timeline.
  unrecognisedFormat,

  /// Fichier au-delà de la limite fixée par l'appelant (§4).
  fileTooLarge,

  /// Échec de lecture.
  io,
}

@freezed
sealed class ImportEvent with _$ImportEvent {
  const ImportEvent._();

  /// Avancement.
  const factory ImportEvent.progress(ImportProgress field0) =
      ImportEvent_Progress;

  /// Lot d'enregistrements à insérer.
  const factory ImportEvent.chunk(ImportChunk field0) = ImportEvent_Chunk;

  /// Import terminé.
  const factory ImportEvent.finished(ImportSummaryOutput field0) =
      ImportEvent_Finished;

  /// Import échoué.
  const factory ImportEvent.failed(ImportErrorKind field0) = ImportEvent_Failed;
}

/// Progression, remontée au fil de la lecture (§4).
class ImportProgress {
  /// Étape en cours.
  final ImportStepKind step;

  /// Avancement, de 0 à 100, si la taille totale est connue.
  final double? percent;

  /// Enregistrements produits jusqu'ici.
  final int records;

  const ImportProgress({
    required this.step,
    this.percent,
    required this.records,
  });

  @override
  int get hashCode => step.hashCode ^ percent.hashCode ^ records.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportProgress &&
          runtimeType == other.runtimeType &&
          step == other.step &&
          percent == other.percent &&
          records == other.records;
}

/// Étape en cours, affichée à l'utilisateur (§4).
enum ImportStepKind {
  /// Détection du format.
  detectingFormat,

  /// Lecture des enregistrements.
  readingRecords,

  /// Lecture terminée.
  done,
}

/// Bilan d'un import réussi — alimente `import_batch` (§5.1).
class ImportSummaryOutput {
  /// Format concret détecté.
  final String format;

  /// Début de la période couverte, en millisecondes UTC.
  final PlatformInt64? periodStartUtc;

  /// Fin de la période couverte, en millisecondes UTC.
  ///
  /// Avec le début, permet d'avertir avant écrasement si le nouvel export
  /// couvre moins que l'actuel (§4).
  final PlatformInt64? periodEndUtc;

  /// Nombre de positions.
  final int pointCount;

  /// Nombre de trajets.
  final int segmentCount;

  /// Nombre d'arrêts.
  final int visitCount;

  /// Enregistrements ignorés — comptés, jamais silencieux (§3.10).
  final int skippedCount;

  /// Octets lus.
  final PlatformInt64 bytesRead;

  const ImportSummaryOutput({
    required this.format,
    this.periodStartUtc,
    this.periodEndUtc,
    required this.pointCount,
    required this.segmentCount,
    required this.visitCount,
    required this.skippedCount,
    required this.bytesRead,
  });

  @override
  int get hashCode =>
      format.hashCode ^
      periodStartUtc.hashCode ^
      periodEndUtc.hashCode ^
      pointCount.hashCode ^
      segmentCount.hashCode ^
      visitCount.hashCode ^
      skippedCount.hashCode ^
      bytesRead.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportSummaryOutput &&
          runtimeType == other.runtimeType &&
          format == other.format &&
          periodStartUtc == other.periodStartUtc &&
          periodEndUtc == other.periodEndUtc &&
          pointCount == other.pointCount &&
          segmentCount == other.segmentCount &&
          visitCount == other.visitCount &&
          skippedCount == other.skippedCount &&
          bytesRead == other.bytesRead;
}

/// Position importée, avant insertion en base.
class ImportedPoint {
  /// Instant UTC, en millisecondes.
  final PlatformInt64 timestampUtc;

  /// Décalage local, en minutes (§5.5).
  final int tzOffsetMinutes;

  /// Latitude.
  final double lat;

  /// Longitude.
  final double lon;

  const ImportedPoint({
    required this.timestampUtc,
    required this.tzOffsetMinutes,
    required this.lat,
    required this.lon,
  });

  @override
  int get hashCode =>
      timestampUtc.hashCode ^
      tzOffsetMinutes.hashCode ^
      lat.hashCode ^
      lon.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportedPoint &&
          runtimeType == other.runtimeType &&
          timestampUtc == other.timestampUtc &&
          tzOffsetMinutes == other.tzOffsetMinutes &&
          lat == other.lat &&
          lon == other.lon;
}

/// Trajet importé, avant insertion en base.
class ImportedSegment {
  /// Début, en millisecondes UTC.
  final PlatformInt64 startTsUtc;

  /// Décalage local au départ.
  final int startTzOffsetMinutes;

  /// Fin, en millisecondes UTC.
  final PlatformInt64 endTsUtc;

  /// Décalage local à l'arrivée.
  final int endTzOffsetMinutes;

  /// Latitude de départ.
  final double startLat;

  /// Longitude de départ.
  final double startLon;

  /// Latitude d'arrivée.
  final double endLat;

  /// Longitude d'arrivée.
  final double endLon;

  /// Distance annoncée, en mètres.
  final double? distanceM;

  /// Mode détecté — clé stable, traduite par l'UI (§3.9).
  final String detectedMode;

  const ImportedSegment({
    required this.startTsUtc,
    required this.startTzOffsetMinutes,
    required this.endTsUtc,
    required this.endTzOffsetMinutes,
    required this.startLat,
    required this.startLon,
    required this.endLat,
    required this.endLon,
    this.distanceM,
    required this.detectedMode,
  });

  @override
  int get hashCode =>
      startTsUtc.hashCode ^
      startTzOffsetMinutes.hashCode ^
      endTsUtc.hashCode ^
      endTzOffsetMinutes.hashCode ^
      startLat.hashCode ^
      startLon.hashCode ^
      endLat.hashCode ^
      endLon.hashCode ^
      distanceM.hashCode ^
      detectedMode.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportedSegment &&
          runtimeType == other.runtimeType &&
          startTsUtc == other.startTsUtc &&
          startTzOffsetMinutes == other.startTzOffsetMinutes &&
          endTsUtc == other.endTsUtc &&
          endTzOffsetMinutes == other.endTzOffsetMinutes &&
          startLat == other.startLat &&
          startLon == other.startLon &&
          endLat == other.endLat &&
          endLon == other.endLon &&
          distanceM == other.distanceM &&
          detectedMode == other.detectedMode;
}

/// Arrêt importé, avant insertion en base.
class ImportedVisit {
  /// Arrivée, en millisecondes UTC.
  final PlatformInt64 arrivalTsUtc;

  /// Décalage local à l'arrivée.
  final int arrivalTzOffsetMinutes;

  /// Départ, en millisecondes UTC.
  final PlatformInt64 departureTsUtc;

  /// Décalage local au départ.
  final int departureTzOffsetMinutes;

  /// Latitude.
  final double lat;

  /// Longitude.
  final double lon;

  /// Rayon annoncé, en mètres.
  final double? radiusM;

  /// Identifiant de lieu propre à la source (§3.1).
  final String? externalPlaceRef;

  const ImportedVisit({
    required this.arrivalTsUtc,
    required this.arrivalTzOffsetMinutes,
    required this.departureTsUtc,
    required this.departureTzOffsetMinutes,
    required this.lat,
    required this.lon,
    this.radiusM,
    this.externalPlaceRef,
  });

  @override
  int get hashCode =>
      arrivalTsUtc.hashCode ^
      arrivalTzOffsetMinutes.hashCode ^
      departureTsUtc.hashCode ^
      departureTzOffsetMinutes.hashCode ^
      lat.hashCode ^
      lon.hashCode ^
      radiusM.hashCode ^
      externalPlaceRef.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is ImportedVisit &&
          runtimeType == other.runtimeType &&
          arrivalTsUtc == other.arrivalTsUtc &&
          arrivalTzOffsetMinutes == other.arrivalTzOffsetMinutes &&
          departureTsUtc == other.departureTsUtc &&
          departureTzOffsetMinutes == other.departureTzOffsetMinutes &&
          lat == other.lat &&
          lon == other.lon &&
          radiusM == other.radiusM &&
          externalPlaceRef == other.externalPlaceRef;
}
