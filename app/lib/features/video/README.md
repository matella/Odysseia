# `video/` — vue Vidéo souvenir (§3.5)

MP4 natif partout, y compris web via `ffmpeg.wasm` (plus lent, accepté).
Personnalisation complète dès la v1 : durée (15/30/45/60/75/90 s), plage de
mois/années, style visuel, template de titre.

L'UI web doit indiquer clairement un temps de rendu plus long (barre de
progression, usage de l'app pendant le rendu si techniquement possible).
